import React from 'react';
import DashBoard from '../DashBoard/DashBoard';

const GiveReview = () => {
    return (
        <div>
            <DashBoard></DashBoard>
            user review
        </div>
    );
};

export default GiveReview;